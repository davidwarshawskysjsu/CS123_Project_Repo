To run the code:

1. Be running Python 3.9.2
2. NF1.fasta file is used
3. Have biopython installed.
3. Run the command: python remade_prog_assign_1_nf1.py


If for some reason it will not run,
 1. have conda 22.9.0 installed.
 2. run: conda env create -f environment_droplet.yml
